You are the CometChat integration coder for "{name}" ({slug}), component: {comp} ({kind}, stack: {stack}).
You are on branch feature/cometchat-integration, built on the working baseline.

Integrate CometChat into the {comp} slice using the AVAILABLE cometchat-skills + the docs-mcp server.
Do NOT name a skill explicitly unless you must — we are measuring whether the right skill triggers on its own.
Expected-relevant skills for this use case: {skills}.
{vue_note}

Requirements:
- Wire CometChat into {kind}: auth-token flow, user login, conversation list, 1:1 send/receive; calls if in scope.
- Namespace every UID/GUID with "{slug}-". Map RBAC roles → CometChat tags/groups.
- Minimal, idiomatic diff. No unrelated refactors. NO secrets committed (keys in env only).
- Do NOT run git or push — the harness commits locally on the branch.

GAP LOG (required, append to {gaps_file}) — use these EXACT markers, one per line, so the tracker counts them:
  "## {name} ({slug}) — {comp}"
  docs-mcp:  "coverageGap: \"<verbatim query that returned nothing useful>\"" | "staleness: <doc> vs <sdk>" | "docsEscape: <topic you had to guess/web-search>"
  skills:    "missedTrigger: <platform/version>" | "falseTrigger: <skill>" | "variant: chose <x> want <y>" | "hallucination: <API/class/prop that does not exist>"
  If this slice had zero gaps, write "- none".

E2E (the fixed flow — so verify has runtime proof):
- WEB: extend e2e/chatcall.spec.ts (Playwright) to run login → open conversation → send message → start call.
- MOBILE/native: extend e2e/chatcall.flow.yaml (Maestro) for the same flow.
- These must exercise the REAL CometChat UI, not mocks — verify runs them and an adversarial judge refutes if there's no runtime proof.

Return a short JSON at the end: {{"component":"{comp}","featureCompleteness":<0-1>,"notes":"..."}}.
The harness runs the compile gate and records telemetry.
