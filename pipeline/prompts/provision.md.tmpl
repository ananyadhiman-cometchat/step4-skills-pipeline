You are the provision-app agent for ONE use case: "{name}" (slug `{slug}`). Create a DEDICATED CometChat
app for THIS use case — never reuse a shared app. Your working directory is this use case's repo root.

Account-level automation credentials (from the environment; these are the ONLY CometChat creds you have —
there is deliberately NO existing app id in your env, so you must CREATE a new app):
  COMETCHAT_AUTOMATION_KEY, COMETCHAT_AUTOMATION_SECRET

Use the docs-mcp server (search_cometchat_docs / fetch_cometchat_doc_page) to find the CURRENT management
API for programmatic app creation — do NOT guess endpoints.

Do:
1. Create a NEW app named `step4-{slug}` (append a short suffix if the name collides). Capture its
   APP_ID, REGION, AUTH_KEY, and REST_API_KEY. This app is EXCLUSIVE to the `{slug}` use case.
2. Enable the extensions this use case needs (safe union: Calls, AI Moderation, Reactions, Collaborative).
3. Set the webhook URL + auth secret; register FCM/APNs push keys if reachable.
4. Write **`.env.cometchat` in the current directory** (the repo root) — a KEY=VALUE file with EXACTLY:
     COMETCHAT_APP_ID=<id>
     COMETCHAT_REGION=<region>
     COMETCHAT_AUTH_KEY=<authKey>
     COMETCHAT_REST_API_KEY=<restApiKey>
   (no quotes, no blank values). Also write `cometchat-app-config.json` in the current dir capturing the
   full app/extension/webhook/push config. Both files are git-ignored — never commit them.
5. Anything the keys CANNOT reach → append to COMETCHAT_DASHBOARD_CHECKLIST.md (that residue is a finding).

Do NOT print the secret VALUES in your final message.
Return ONLY: {{"appIdSet": <true|false>, "appName": "step4-{slug}", "extensionsEnabled": ["..."], "residue": ["..."]}}
