# Mobile as CALLER: open the conversation with ${PEER} and start a call.
# Params: -e APP_ID=... -e PEER="<peer display name>"
appId: ${APP_ID}
---
- tapOn: "${PEER}"
- assertVisible: { id: "cometchat-message-list" }
- tapOn: { id: "cometchat-call-button" }
- takeScreenshot: /tmp/mobile-outgoing
