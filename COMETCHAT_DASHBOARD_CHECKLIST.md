# CometChat Dashboard Checklist — residue from provision-app

App: **STEP4-sweep-shared** · appId `1680742d290138a01` · region `us` · plan trial

Everything below is what the **automation/management keys could NOT reach**. Each item
is itself a finding: it marks a boundary of the programmatic surface. Resolve in the
[dashboard](https://app.cometchat.com/) or by supplying the missing env vars and re-running.

## 1. Calls (Voice & Video) — NOT an extension
- Calls is a **core / plan-gated capability**, not a CometChat extension and not a settings
  toggle. There is **no management-API enable** for it. It is available by default when the
  plan entitles it.
- **Action:** Confirm the app's plan includes Voice & Video Calling, and configure any
  call-specific options (default call settings, **call recording**, RTC region) in the
  dashboard — these have no management-API surface.

## 2. Webhook — NOT registered
- `COMETCHAT_WEBHOOK_URL` and `COMETCHAT_WEBHOOK_SECRET` are **empty** in `.env.pipeline`;
  there was no URL to register, so no webhook was created.
- Endpoint is available once a URL exists:
  `POST https://apimgmt.cometchat.io/apps/{appId}/webhooks`
  body `{id, name, webhookURL, useBasicAuth:true, username, password, enabled:true}`
  (recommend Basic Auth; use `X-Webhook-Version: 2` header for the new webhook engine).
- **Action:** Fill `COMETCHAT_WEBHOOK_URL` + `COMETCHAT_WEBHOOK_SECRET` and re-run, or add
  the webhook in **Application → Webhooks** (enable Moderation triggers for AI-moderation events).

## 3. Push — FCM NOT registered
- `COMETCHAT_FCM_SERVER_KEY` empty and no Firebase service-account file present.
- **Action:** Add the FCM provider (service-account JSON / server key) under
  **Notifications → Push** in the dashboard, then register device tokens client-side.

## 4. Push — APNs NOT registered
- `COMETCHAT_APNS_KEY_ID` / `COMETCHAT_APNS_TEAM_ID` empty and no `.p8` auth-key file present.
- **Action:** Upload the APNs `.p8` key (Key ID + Team ID + Bundle ID) under
  **Notifications → Push** in the dashboard.

## 5. AI Moderation — toggle ON, rules/AI-context still to configure
- `features.moderation.advanced.enabled = true` was set via the management settings API, so
  Advanced (AI) Moderation is **enabled**.
- **Residue:** The moderation **rules** (enable defaults or create custom), keyword lists, and
  the **AI image/video OpenAI context** are configured via the dashboard
  (**Application → Moderation → Rules**) or the app-level Moderation REST API
  (`/rest-api/moderation-apis`), not by the management keys used here.

---
_Generated by the provision-app agent (one-time, whole-sweep). Reachable steps were already
applied programmatically; see `cometchat-app-config.json`._
